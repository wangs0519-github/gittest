cd gitdir

#把这个文件夹变成Git可管理的仓库,chuang jian bendi仓库
git init

#添加文件夹下的所有文件到暂存区里面去
git add .

#把项目提交到仓库
git commit -m "第一次提交"

#chuangjian SSH Key 一路回车.ssh目录里找到id_rsa和id_rsa.pub这两个文件
ssh-keygen -t rsa -C "youremail@example.com

#登录Github --->点击右上角的图标 --->选择Settings --->点击左边的SSH and GPG KEYS --->点击右上角的New SSH key

#测试ssh key是否成功，使用命令“ssh -T git@github.com”,You’ve successfully authenticated, but GitHub does not provide shell access
ssh -T git@github.com

#先删除以防其他的远程链接占用
git remote rm origin 

#和本地仓库进行关联
git remote add origin git@github.com:linsili/test.git

#将本地库的所有内容推送到远程仓
#由于新建的远程仓库是空的，所以要加上-u这个参数，等远程仓库里面有了内容之后，下次再从本地库上传内容的时候只需 git push origin master

git push -u origin master

#zhu:ruguo新创建的那个仓库里面的README文件不在本地仓库目录中，这时我们可以通过git pull --rebase origin master命令先将内容合并,此时再push就能成功了

#zhu:git status  +  git diff  + git merge:hebin + git clone + git log +
git branch –a + git checkout ver2.0:qiehuanfenzhi






